import React from 'react';

const GridViewTable = ({
                           ...rest
                       }) => {
    return (
        <div>
            table
        </div>
    );
};

export default GridViewTable;