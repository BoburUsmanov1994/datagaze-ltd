import React from 'react';

const GridViewTimeline = ({
                              ...rest
                          }) => {
    return (
        <div>
            timeline
        </div>
    );
};

export default GridViewTimeline;